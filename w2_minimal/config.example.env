# Máy chạy benchmark là client; đây là máy Linux đích.
SERVER_USER=pi
SERVER_HOST=CHANGE_ME
SERVER_PORT=
RUN_ID=
NETWORK_PROFILE=unspecified

# Ma trận thí nghiệm.
PROTOCOLS=ssh3,ssh,mosh
WORKLOADS=find_usr,docker_logs,journalctl,large_file
TRIALS_PER_COMBINATION=5
RANDOM_SEED=20260724
WARMUP_SECONDS=2.0
INTER_TRIAL_DELAY_SECONDS=3.0

# Timeout toàn phép đo và timeout khi stream đã ngừng sinh dữ liệu.
SESSION_TIMEOUT=20.0
SAMPLE_TIMEOUT=180.0
COMMAND_IDLE_TIMEOUT=20.0
MAX_READ_BYTES=65536
# 0 nghĩa là nhận toàn bộ output; số dương chủ động giới hạn bằng head.
MAX_OUTPUT_LINES=0

# Lệnh trên target. Docker mặc định dùng container đang chạy đầu tiên.
FIND_COMMAND=find /usr
DOCKER_LOGS_COMMAND=docker logs $(docker ps -q | head -n 1)
JOURNALCTL_COMMAND=journalctl --no-pager
LARGE_FILE_PATH=/tmp/w2_large_file.txt
LARGE_FILE_SIZE_BYTES=16777216
PREPARE_LARGE_FILE=1
REMOTE_PREPARE_SCRIPT=/tmp/w2_prepare_large_file.sh

RESULT_DIR=artifacts/results
FIGURE_DIR=artifacts/figures
LIVE_PROGRESS=1
TERMINAL_TYPE=xterm-256color
AUTO_PLOT=1
PLOT_METRICS=mean,median,p90,p95

SSH_BIN=ssh
SSH_IDENTITY_FILE=~/.ssh/id_ed25519
SSH_STRICT_HOST_KEY_CHECKING=0
SSH_BATCH_MODE=1

SSH3_BIN=ssh3
SSH3_INSECURE=1
SSH3_PRIVKEY=~/.ssh/id_ed25519
SSH3_PORT=4433
SSH3_PATH=/ssh3-term
SSH3_EXTRA_ARGS=

MOSH_BIN=mosh
MOSH_PREDICT=always
MOSH_EXTRA_ARGS=

PYTHON_BIN=.venv/bin/python

